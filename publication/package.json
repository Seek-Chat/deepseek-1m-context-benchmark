{
  "name": "deepseek-1m-context-publication-pipeline",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "scaffold": "node build_release.mjs scaffold",
    "build": "node build_release.mjs build"
  },
  "engines": {
    "node": ">=20"
  }
}
